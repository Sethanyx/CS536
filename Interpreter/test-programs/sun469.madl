fn main(): int {
    x:int;
    y:int = 20;
    output(x+y);
    if (x > y) {
        output(x - y);
    } else {
        output(y - x);
    }
    {
        x:int = 30;
        output(x);
    }
    while (x > 0) {
        x = x - 2;
    }
    return 0;
}